# NOT-KEYLESS — free tiers that need a signup/key (one line each)

These are NOT in the keyless registry. All have free tiers but require account signup + API key — no dollars, but not zero-touch. The owner's action list is signups, not spending.

## Inference (free tier behind free signup)
- Hetzner Inference API (EU) — free while experimental, 500M in/5M out tok/day, Qwen3.6-35B+vision, OpenAI-compatible — HIGHEST-VALUE signup for lab's 2nd keyed family
- OpenRouter `:free` models — 20 free-tagged models, needs free key
- Groq — free tier, needs signup+key
- Cerebras — free tier, needs signup+key
- SambaNova Cloud — free tier, needs signup+key (models catalog is public)
- Together AI — free credits, needs signup+key
- Fireworks — free tier, needs signup+key
- DeepInfra — free credits, needs signup+key
- Hyperbolic — free trial credits, needs signup+key
- Nebius — free credits, needs signup+key
- Chutes — needs signup+key
- Venice — needs signup+key
- Mistral — free tier (+phone verify), needs signup+key
- Cohere — free trial key, needs signup
- Google AI Studio (Gemini) — free tier, needs signup+key
- NVIDIA NIM — free tier, needs signup+key
- Cloudflare Workers AI — free tier, needs account
- DeepSeek — needs signup+key
- Qwen/DashScope — needs Alibaba Cloud signup+key
- Moonshot/Kimi — needs signup+key
- Zhipu GLM — needs signup+key
- MiniMax — needs signup+key
- SiliconFlow — needs signup+key
- Novita — needs signup+key
- Infermatic — needs signup+key
- Kluster.ai — needs signup+key
- Featherless — needs signup+key
- Aleph Alpha — needs signup+key
- AI21 — needs signup+key
- TextSynth — free tier behind key
- HuggingChat — login required for inference
- LMSYS/Arena — login required
- Poe — login required
- Phind — login required
- Kilo AI Gateway — "anonymous" docs claim FALSE: API returns 401 PAID_MODEL_AUTH_REQUIRED "You need to sign in" (verified 2026-09-16)
- ApiAirforce — 612-model catalog is public but chat/completions 401s "Missing Authorization" (verified 2026-09-16)
- Pollinations new API (gen.pollinations.ai/v1, 372 models) — 401 "valid API key required"; only the legacy text.pollinations.ai/openai endpoint stays anonymous
- HuggingChat — login required for inference (verified 2026-09-16)
- ApiAirforce — catalog public (612 models) but chat 401 "Missing Authorization" (verified 2026-09-16)
- L402/Lightning inference (api.nautdev.com) — no signup/key BUT pay-per-request in sats, not $0

## Hosting / compute / storage (free tier behind signup)
- Cloudflare Pages+Workers, Vercel, Netlify, Deno Deploy, Fly.io, Oracle Always-Free, GCP free tier, Replit, Glitch
- Supabase, Turso, Neon, Upstash, Cloudflare R2/KV

## Search / data (free tier behind key)
- Brave Search, Tavily, Jina Search grounding (Reader stays keyless)

## Payments
- Stripe test mode (needs API key); Base Sepolia faucets (need account signup)

*Rule: a signup completed by the owner promotes the candidate into the keyless registry only with a live no-auth receipt — until then it stays here.*
